# Saran UI/UX #2 — Visualisasi Data & Readability

**Target:** Layar ranking, recap, dan hasil pencarian.

## 2.1 Bar chart skor per peserta di layar ranking
Sekarang ranking hanya tabel angka. Tambahkan bar horizontal kecil di kolom skor
agar perbandingan antar peserta langsung terlihat.
- **Valid:** YA — belum diimplementasikan. `tui_progress_bar` sudah ada, tinggal
  panggil dengan skala relatif terhadap skor maksimum.
- **Tindakan:** Di `surfaces_ranking_command.c`, setelah kolom skor, gambar
  `tui_progress_bar(row, col, 20, score*100/max_score, COLOR_SUCCESS)`.

## 2.2 Warna zona di riwayat (sudah sebagian ada, perlu konsisten)
Di scoring, riwayat zona sudah diwarnai (hijau/merah). Di ranking/recap, kolom
frekuensi zona masih putih polos.
- **Valid:** YA — konsistensi visual.
- **Tindakan:** Di recap, cetak frekuensi zona 5 dengan `COLOR_SUCCESS` (bold),
  zona 0 dengan `COLOR_ERROR`, sisanya `COLOR_MENU`.

## 2.3 Pembatas tabel yang lebih jelas di recap
Tabel recap panjang (hingga 7 peserta) sulit dibaca mata karena tiap baris polos.
- **Valid:** YA.
- **Tindakan:** Gandakan garis pemisah antar peserta (sudah ada `tui_separator`),
  atau beri warna latar selang-seling (zebra striping) menggunakan `COLOR_DIM`
  sebagai latar baris genap.

## 2.4 Ringkasan statistik (sudah dikerjakan)
Recap sudah menampilkan "Peserta | Total Skor | Rata-rata | Tertinggi" — ini
hasil sesi hari ini. REDUNDAN, tidak perlu diulang.

## 2.5 Medal di ranking (sudah ada)
`tui_medal(r->rank)` sudah dipasang di ranking & recap. REDUNDAN.

## 2.6 Panel juara 1 di menu utama (sudah dikerjakan di sesi ini)
Menu utama sudah menampilkan "Juara 1: <nama> (<skor>)" saat lomba selesai.
REDUNDAN.
